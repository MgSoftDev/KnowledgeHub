@page "/kh/permissions/{PagePk:guid}"

@* Página envoltorio (opt-in vía AdditionalAssemblies). NO declara @layout a propósito:
   así adopta el DefaultLayout del anfitrión. Ruta DEBE coincidir con KnowledgeHubRoutes.Permissions. *@

<KnowledgeHubPagePermissions PagePk="@PagePk" />

@code {
    [Parameter] public Guid PagePk { get; set; }
}
